node_modules
.next
.next-second
out
next-env.d.ts
public
pnpm-lock.yaml
playwright-report
test-results
blob-report

# Generated by `supabase gen types typescript --linked` — reformatting it
# makes every regeneration produce a spurious diff (AGENTS.md: never hand-edit).
src/shared/api/database.types.ts

# `next dev` rewrites the <!-- BEGIN:nextjs-agent-rules --> block from a
# fixed template (node_modules/next/dist/server/lib/generate-agent-files.js).
# Prettier-formatting it means every `pnpm dev` re-dirties the tree.
# CLAUDE.md is a symlink to AGENTS.md — listed so it isn't formatted twice.
AGENTS.md
CLAUDE.md
