name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm typecheck

      - run: pnpm lint

      - run: pnpm lint:css

      - run: pnpm format:check

      # `pnpm build` is deliberately not run here: generateStaticParams and
      # app/sitemap.ts hit the live Supabase project at build time (see
      # AGENTS.md's rendering-mode table), which would need production
      # secrets in CI. typecheck/lint/lint:css/format:check are what's
      # checked until that's set up.
